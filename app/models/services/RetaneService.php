<?php
class RetaneService {
    /*
    |--------------------------------------------------------------------------
    | Retane Service
    |--------------------------------------------------------------------------
    |
    | Berisi fungsi-fungsi pembantu
    |
    */
    public static function CheckExistingFolder($PATH)
    {
        // Create a directory
        //File::mkdir($path);
        // Recursively delete a directory
        //File::rmdir($directory);
        // Delete a file
        //File::delete($path);
    }
}